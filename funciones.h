#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED
float suma(float num1, float num2);
/** \brief El usuario ingresa datos y se realiza la suma entre los numeros ingresados
 *
 * \param num1: Se carga el numero a la variable.
 * \param num2: Se carga el numero a la variable.
 * \return Devuelve el resultado de la suma entre las dos variables cargadas.
 *
 */

float resta (float num1, float num2);
/** \brief El usuario ingresa datos y se realiza la resta entre los numeros ingresados
 *
 * \param num1: Se carga el numero a la variable.
 * \param num2: Se carga el numero a la variable.
 * \return Devuelve el resultado de la resta entre las dos variables cargadas.
 *
 */

float division (float num1, float num2);
/** \brief El usuario ingresa datos y se realiza la division entre los numeros ingresados
 *
 * \param num1: Se carga el numero a la variable.
 * \param num2: Se carga el numero a la variable.
 * \return Devuelve el resultado de la division entre las dos variables cargadas.
 *
 */

float multiplicacion (float num1, float num2);
/** \brief El usuario ingresa datos y se realiza la multiplicacion entre los numeros ingresados
 *
 * \param num1: Se carga el numero a la variable.
 * \param num2: Se carga el numero a la variable.
 * \return Devuelve el resultado de la resta entre las dos variables cargadas.
 *
 */
float factorial(float num1);
/** \brief El usuario ingresa datos y se realiza el factoreo del numero ingresado
 *
 * \param num1: Se carga el numero a la variable.
 * \return Devuelve el factoreo del numero ingresado.
 *
 */


#endif // FUNCIONES_H_INCLUDED
